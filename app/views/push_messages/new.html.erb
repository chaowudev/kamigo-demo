<%= form_with(url: '/push_messages', local: true) do |form| %>
  <div class="field">
    <%= form.label :text %>
    <%= form.text_field :text %>
  </div>

  <div class="actions">
    <%= form.submit I18n.t('common.submit') %>
  </div>
<% end %>
